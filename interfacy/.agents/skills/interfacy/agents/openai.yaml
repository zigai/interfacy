interface:
  display_name: "Interfacy"
  short_description: "Use when building or refactoring a Python CLI from typed functions, classes, or instances with Interfacy."
  default_prompt: "Use $interfacy when building or refactoring a typed Python CLI with Interfacy."
policy:
  allow_implicit_invocation: true
